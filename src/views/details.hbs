
            <section id="details-page">
                <h1>Details</h1>
                <article class="details-card">

                    <article class="details-card-text">
                        <h2>Title:<br> {{publication.title}}</h2>
                        <h3>Author: {{publication.author.username}}</h3>
                        <h3>Painting technique: {{publication.technique}}</h3>
                        <h3>Certificate of authenticity: {{publication.authenticity}}</h3>

                        <!-- If there is no registered user, do not display buttons-->
                        {{#if isAuthenticated}}
                        <div class="buttons">
                            <!-- Only for registered user and author of the publication -->
                            {{#if isOwner}}
                            <a href="/edit/{{publication._id}}" class="btn-edit">Edit</a>
                            <a href="/delete/{{publication._id}}" class="btn-delete">Delete</a>
                            {{else}}
                            {{#if hasShared}}
                            <!-- logged in user who has already shared the publication-->
                            <p class="shared-pub">You already shared this publication</p>
                            {{else}}
                            <!-- logged in user who has not yet shared the publication-->
                            <a href="/share/{{publication._id}}" class="btn-share">Share publication</a>
                            {{/if}}
                            {{/if}}
                        </div>
                        {{/if}}

                    </article>

                    <article class="details-card-image">
                        <img src="{{publication.artPicture}}" alt="art-image2">
                    </article>

                </article>
            </section>
       